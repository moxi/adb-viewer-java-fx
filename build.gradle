buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath group: 'de.dynamicfiles.projects.gradle.plugins', name: 'javafx-gradle-plugin', version: '8.4.0'
    }
}

allprojects {
    repositories {
        jcenter()

        mavenCentral()
    }
}
